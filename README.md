# README

- `rails new friends` to create a new Rails application

- `rails s` to start the server

- `rails g controller home index` to create a new controller and action

- `rails routes` to see the routes

- `rails g scaffold friends first_name:string last_name:string email:string phon
e:string twitter:string`  to create a new scaffold for the friends model

- `rails db:migrate` to migrate the database